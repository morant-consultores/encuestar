% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/drmnar_diagnostico.R
\name{diagnosticar_norespuesta}
\alias{diagnosticar_norespuesta}
\title{Diagnóstico de no respuesta no ignorable por pregunta y subconjunto}
\usage{
diagnosticar_norespuesta(
  diseno,
  preguntas,
  covariables = NULL,
  instrumento = "drmnar_z",
  respuesta_ind = NULL,
  subconjuntos = NULL,
  nivel_confianza = 0.95,
  ...
)
}
\arguments{
\item{diseno}{Objeto \code{survey::svydesign} del flujo de encuestar.}

\item{preguntas}{Preguntas a diagnosticar. Puede ser un vector de
nombres (variables ya dicotómicas 0/1) o una lista nombrada
\code{list(pregunta = categoria(s))}; con varias categorías se produce una
estimación por categoría (una-vs-resto).}

\item{covariables}{Vector de covariables para los modelos de propensión
e imputación (demográficas + contextuales por sección).}

\item{instrumento}{Columna con el brazo del instrumento aleatorizado.}

\item{respuesta_ind}{Columna con el indicador de respuesta del ítem;
\code{NULL} lo deriva de los NA de la pregunta.}

\item{subconjuntos}{Lista nombrada de vectores lógicos (\code{NULL} en un
elemento = todas las observaciones). Default: un solo grupo "Estado".}

\item{nivel_confianza}{Nivel del intervalo para la prueba de
ignorabilidad (default 95\%).}

\item{...}{Argumentos adicionales para \code{\link[=estimar_drmnar]{estimar_drmnar()}}.}
}
\value{
Tibble con una fila por pregunta x categoría x subconjunto:
\code{pregunta}, \code{categoria}, \code{subconjunto}, \code{gamma_y}, \code{ee}, \code{inf}, \code{sup},
\code{z_stat}, \code{no_ignorable}, \code{decision} ("DR-MNAR", "Raking" o
"Sin estimación"), \code{n}, \code{convergencia}, \code{est_drmnar}, \code{est_rake},
\code{diferencia} (cuánto se modifica la estimación al corregir por MNAR).
}
\description{
Estima el parámetro crítico de no ignorabilidad \code{gamma_y} (vía el
estimador doblemente robusto de \code{\link[=estimar_drmnar]{estimar_drmnar()}}) para cada pregunta
y subconjunto solicitado, y aplica la regla de decisión DR-MNAR vs
Raking de la Sección 5 del Anexo Técnico.
}
\details{
El diagnóstico por subconjunto importa porque el sesgo puede cancelarse
globalmente y ser severo dentro de un subgrupo (o tener signos opuestos
entre subgrupos).
}
\examples{
\dontrun{
diagnosticar_norespuesta(
  diseno = encuesta$muestra$diseno,
  preguntas = list(
    "conoce_cruz" = "Sí lo conoce",
    "conoce_andrea" = "Sí lo conoce"
  ),
  covariables = c("sexo", "rango_edad"),
  subconjuntos = list(
    "Estado" = NULL,
    "Juárez" = datos$municipio == "JUAREZ"
  )
)
}
}
