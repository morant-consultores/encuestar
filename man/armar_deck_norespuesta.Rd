% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deck_norespuesta.R
\name{armar_deck_norespuesta}
\alias{armar_deck_norespuesta}
\title{Deck de diagnóstico de no respuesta no ignorable (pptx)}
\usage{
armar_deck_norespuesta(
  diseno,
  bundle,
  salida,
  plantilla = NULL,
  titulo = "Diagnóstico de no respuesta (DR-MNAR)",
  subtitulo = "",
  preguntas_clave = NULL,
  subgrupos = NULL,
  catalogo_covariables = NULL,
  nota = NULL,
  top_gammas = 30,
  metodo_multiplicidad = "BH",
  layout = "una_graf",
  master = "Tema de Office"
)
}
\arguments{
\item{diseno}{Objeto \code{survey::svydesign} del estudio.}

\item{bundle}{Objeto \code{diseno_drmnar} de \code{\link[=generar_diseno_drmnar]{generar_diseno_drmnar()}}.}

\item{salida}{Ruta del \code{.pptx} a escribir.}

\item{plantilla}{Ruta al \code{.pptx} plantilla; \code{NULL} usa la default de officer.}

\item{titulo, subtitulo}{Textos de la portada.}

\item{preguntas_clave}{Preguntas headline que se grafican como REFERENCIA
aunque su no respuesta sea ignorable (control de que los siete estimadores
coincidan cuando gamma ~ 0).}

\item{subgrupos}{Lista nombrada de vectores lógicos para la lámina de
heterogeneidad. \code{NULL} (default) omite la lámina; encenderla recalcula el
diagnóstico por subgrupo, que es caro.}

\item{catalogo_covariables}{\code{data.frame} con \code{covariable}, \code{tipo}, \code{fuente} y
\code{mide}; \code{NULL} usa el \code{covariables_doc} del bundle.}

\item{nota}{Nota al pie de cada lámina.}

\item{top_gammas}{Máximo de preguntas en el caterpillar.}

\item{metodo_multiplicidad}{Método de \code{\link[=ajustar_multiplicidad_norespuesta]{ajustar_multiplicidad_norespuesta()}}.}

\item{layout, master}{Layout y master de la plantilla.}
}
\value{
La ruta de \code{salida}, invisible.
}
\description{
Arma el deck completo del diagnóstico DR-MNAR desde el paquete: las láminas
que van del diseño y su validez al dictamen de ponderación, con las lecturas
de \code{gamma} en lenguaje de negocio.
}
\details{
Antes de existir esta función cada estudio portaba su propio script de ~230
líneas; ya habían divergido entre sí y dos láminas se armaban con \code{officer}
inline, fuera del contrato de que todo el graficado sale del paquete. Lo que
cambia entre estudios entra por parámetro.

El orden de las láminas cuenta el argumento completo: diseño y validez ->
diagnóstico e impacto -> profundización y costo -> validación robusta ->
dictamen. La validez del instrumento va ANTES del diagnóstico porque si no se
sostiene, no hay nada que leer después.

Toda lámina cuyo insumo salga vacío se OMITE en vez de reventar, para que el
deck sirva a estudios en distintas etapas de campo.
}
