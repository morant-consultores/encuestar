% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/drmnar_integracion.R
\name{ajustar_pesos_drmnar}
\alias{ajustar_pesos_drmnar}
\title{Diseño repesado por la propensión MNAR de una pregunta (weight-swap)}
\usage{
ajustar_pesos_drmnar(
  diseno,
  pregunta,
  covariables = NULL,
  instrumento = "drmnar_z",
  respuesta_ind = NULL,
  categoria = NULL,
  gamma_inicial = 1.1,
  intervalo_gamma = c(-5, 8)
)
}
\arguments{
\item{diseno}{Objeto \code{survey::svydesign} del flujo de encuestar (con
pesos, UPM y estratos declarados).}

\item{pregunta}{Nombre de la variable de opinión a estimar.}

\item{covariables}{Vector de nombres de covariables (demográficas y
contextuales censales/electorales por sección). \code{NULL} usa solo la
constante.}

\item{instrumento}{Nombre de la columna con el brazo del instrumento
aleatorizado (0 = control, 1 = tratamiento).}

\item{respuesta_ind}{Nombre de la columna con el indicador de respuesta
del ítem; \code{NULL} lo deriva de los NA de la pregunta.}

\item{categoria}{Valor (o valores) de la pregunta que definen \code{y = 1},
p. ej. \code{"Sí lo conoce"}. Innecesario si la pregunta ya es 0/1.}

\item{gamma_inicial}{Valor inicial de gamma para el solucionador.}

\item{intervalo_gamma}{Intervalo de búsqueda de gamma.}
}
\value{
Objeto \code{survey::svydesign} con los pesos ajustados (la variable
\code{peso_drmnar} queda en las variables del diseño para auditoría).
}
\description{
Estima el modelo de propensión MNAR de la pregunta con el instrumento
aleatorizado y devuelve un nuevo \code{survey::svydesign} cuyo peso es el
peso base multiplicado por \code{1/pi} en los respondientes del ítem. El
diseño resultante entra al flujo de morantviz SIN cambios
(\code{Graficar$new(diseno = ...)}) y sus medias ponderadas equivalen al
estimador IPW-MNAR (Weights-MNAR de \code{\link[=estimar_drmnar]{estimar_drmnar()}}).
}
\details{
El peso ajustado es específico de la pregunta: gamma_y difiere por
pregunta, así que no existe un único diseño corregido para todo el
cuestionario.
}
\examples{
\dontrun{
dis_dr <- ajustar_pesos_drmnar(
  diseno, pregunta = "conoce_cruz", categoria = "Sí lo conoce",
  covariables = c("sexo", "rango_edad")
)
g <- morantviz::Graficar$new(diseno = dis_dr, ...)
}
}
