% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diseno_capas.R
\name{construir_diseno_capas}
\alias{construir_diseno_capas}
\title{Diseño muestral por capas: selección planeada + clases de no respuesta + rake}
\usage{
construir_diseno_capas(
  bd,
  plan,
  seccion = "SECCION",
  tope_clase = 3,
  margenes = NULL,
  estrato = NULL,
  umbral_rake = c(0.5, 2)
)
}
\arguments{
\item{bd}{Snapshot de entrevistas efectivas (una fila por entrevista).}

\item{plan}{Tibble del plan muestral por sección con columnas
\code{seccion}, \code{pi_seccion} (probabilidad de inclusión del sorteo),
\code{ln_seccion} (lista nominal o medida de tamaño; opcional pero
recomendada) y \code{n_plan} (entrevistas planeadas).}

\item{seccion}{Nombre de la columna de sección en \code{bd}.}

\item{tope_clase}{Tope del factor de clase de no respuesta
(default 3: una sección con menos de un tercio del plan no infla más).}

\item{margenes}{Lista nombrada de data.frames \verb{variable, Freq} para el
rake (p. ej. \code{list(sexo = ..., rango_edad = ...)}). \code{NULL} = sin rake.}

\item{estrato}{Columna de estrato en \code{bd} (opcional).}

\item{umbral_rake}{Rango aceptable de los factores de rake
(composición); fuera de él se emite warning operativo.}
}
\value{
\code{survey.design} con UPM = sección y atributo \code{"capas"}: tabla
por sección con \code{w_seleccion}, \code{logradas}, \code{factor_clase} (auditoría
de cada capa).
}
\description{
Construye el \code{survey::svydesign} de la nueva arquitectura de pesos a
partir del snapshot de efectivas y el plan muestral por sección,
reemplazando la ruta del \code{fpc} empírico (que convertía la no respuesta
en "etapa de muestreo" bajo un supuesto MCAR refutado por el
diagnóstico DR-MNAR).
}
