% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/drmnar_estimacion.R
\name{estimar_drmnar}
\alias{estimar_drmnar}
\title{Estimación doblemente robusta bajo no respuesta no ignorable (DR-MNAR)}
\usage{
estimar_drmnar(
  diseno,
  pregunta,
  covariables = NULL,
  instrumento = "drmnar_z",
  respuesta_ind = NULL,
  categoria = NULL,
  subconjunto = NULL,
  nombre_subconjunto = "Estado",
  gamma_inicial = 1.1,
  intervalo_gamma = c(-5, 8),
  nivel_confianza = 0.95
)
}
\arguments{
\item{diseno}{Objeto \code{survey::svydesign} del flujo de encuestar (con
pesos, UPM y estratos declarados).}

\item{pregunta}{Nombre de la variable de opinión a estimar.}

\item{covariables}{Vector de nombres de covariables (demográficas y
contextuales censales/electorales por sección). \code{NULL} usa solo la
constante.}

\item{instrumento}{Nombre de la columna con el brazo del instrumento
aleatorizado (0 = control, 1 = tratamiento).}

\item{respuesta_ind}{Nombre de la columna con el indicador de respuesta
del ítem; \code{NULL} lo deriva de los NA de la pregunta.}

\item{categoria}{Valor (o valores) de la pregunta que definen \code{y = 1},
p. ej. \code{"Sí lo conoce"}. Innecesario si la pregunta ya es 0/1.}

\item{subconjunto}{Vector lógico para estimar sobre un subgrupo (puede
haber MNAR por subgrupo aunque no lo haya globalmente).}

\item{nombre_subconjunto}{Etiqueta del subgrupo en la salida.}

\item{gamma_inicial}{Valor inicial de gamma para el solucionador.}

\item{intervalo_gamma}{Intervalo de búsqueda de gamma.}

\item{nivel_confianza}{Nivel de los intervalos (default 95\%).}
}
\value{
Tibble con columnas \code{modelo}, \code{est}, \code{ee}, \code{inf}, \code{sup},
\code{gamma_y}, \code{ee_gamma_y}, \code{pregunta}, \code{categoria}, \code{subconjunto},
\code{n}, \code{convergencia}.
}
\description{
Calcula la comparación completa de estimadores de la Figura 1 de Bailey
(2023) para una pregunta dicotomizada de la encuesta, incorporando los
pesos del diseño muestral complejo y la varianza sándwich agrupada por
UPM y estrato:
}
\details{
\itemize{
\item \code{Observado}: media simple de los respondientes (sin pesos).
\item \code{Weights-MAR}: media ponderada por el diseño (equivale a
\code{survey::svymean} con el diseño rakeado — el flujo actual de Morant).
\item \code{Imput-MAR} / \code{DR-MAR}: imputación y doblemente robusto asumiendo
ignorabilidad (gamma = 0).
\item \code{Weights-MNAR} / \code{Imput-MNAR} / \code{DR-MNAR}: los tres estimadores de
Bailey con gamma estimado mediante el instrumento aleatorizado.
}

El peso analítico de los estimadores MNAR es el peso base del diseño
multiplicado por la propensión inversa \code{1/pi_i}, y en el DR el peso se
aplica sobre el residuo \code{(y - y_imputada)} (Sección 6 del Anexo Técnico).
}
\examples{
\dontrun{
estimar_drmnar(
  diseno = encuesta$muestra$diseno,
  pregunta = "conoce_cruz",
  covariables = c("sexo", "rango_edad", "escolaridad_prom"),
  categoria = "Sí lo conoce"
)
}
}
