% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registro_contactos.R
\name{derivar_registro_contactos}
\alias{derivar_registro_contactos}
\title{Derivar el registro de contactos por sección (insumo de la sobremuestra)}
\usage{
derivar_registro_contactos(
  bd,
  id = "SbjNum",
  seccion = "SECCION",
  prefijo = NULL,
  max_intentos = 15,
  codigos = NULL,
  col_intento_efectivo = "INT"
)
}
\arguments{
\item{bd}{Snapshot de entrevistas (una fila por renglón levantado).}

\item{id}{Columna que identifica el renglón (default \code{"SbjNum"}).}

\item{seccion}{Columna de la sección (default \code{"SECCION"}).}

\item{prefijo}{Prefijo de las columnas de intentos. \code{NULL} (default)
autodetecta: primero \code{INT_}, luego \code{INT}.}

\item{max_intentos}{Máximo de intentos registrables (default 15, el
acuerdo con campo).}

\item{codigos}{Catálogo opcional para recodificar resultados: vector
nombrado \code{c("1" = "Efectiva", "2" = "No abrió", ...)}. Los códigos
fuera del catálogo se conservan crudos y se avisa (no se pierde
información).}

\item{col_intento_efectivo}{Columna de respaldo con el número del
intento efectivo (default \code{"INT"}).}
}
\value{
\code{tibble} con una fila por sección: \code{seccion} (caracter),
\code{contactos}, \code{efectivas} y, con detalle, una columna
\verb{intentos_<resultado>} por cada resultado observado (solo intentos
registrados; puede sumar menos que \code{contactos}). Atributo
\code{"fuente"}: \code{"detalle_intentos"} o \code{"intento_efectivo"}.

Garantías del contrato: \code{contactos >= efectivas} siempre (cada
renglón levantado cuenta al menos su propio contacto, aunque el
encuestador no haya registrado intentos), ninguna sección del
snapshot desaparece, y los renglones sin sección se excluyen con
aviso.
}
\description{
Agrega los intentos de entrevista a la tabla por sección que consume
\code{muestreaR::planear_siguiente_ola()}: \code{seccion}, \code{contactos} (todos
los toques de puerta registrados), \code{efectivas} (renglones levantados)
y el desglose por resultado (\code{intentos_no_abrio}, \code{intentos_rechazo},
...). Con ella la sobremuestra de la ola siguiente se dimensiona por
la tasa de respuesta REAL de cada sección.
}
\details{
Dos fuentes, en orden de preferencia:
\describe{
\item{detalle por intento}{columnas \code{INT_1..INT_15} / \code{INT1..INT15}
(vía \code{\link[=pivotar_intentos]{pivotar_intentos()}}): contactos y desglose completos.}
\item{respaldo \code{INT}}{cuando el detalle no existe o viene vacío
(olas previas al acuerdo, p. ej. Chihuahua sep-2025 y feb-2026):
\code{INT} es el número del intento en que se logró la entrevista, así
que \code{contactos = sum(INT)} con \code{INT} faltante contado como 1. Se
avisa la limitación: sin tipos de fallo y sin las viviendas donde
nunca se logró entrevista.}
}
}
