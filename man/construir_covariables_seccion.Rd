% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/drmnar_covariables.R
\name{construir_covariables_seccion}
\alias{construir_covariables_seccion}
\title{Covariables contextuales por sección electoral para DR-MNAR}
\usage{
construir_covariables_seccion(
  censo_seccion,
  electoral_bd = NULL,
  elecciones = NULL,
  marginacion = NULL
)
}
\arguments{
\item{censo_seccion}{Censo por sección (llave \code{seccion} = "EE_SSSS").}

\item{electoral_bd}{Histórico electoral por sección (opcional).}

\item{elecciones}{Elecciones a promediar (requerido con \code{electoral_bd}).}

\item{marginacion}{Tibble opcional \code{seccion} + \code{indice_marginacion}
(CONAPO; no forma parte del censo).}
}
\value{
Tibble con una fila por sección.
}
\description{
Construye la tabla de covariables seccionales de la Sección 4 del Anexo
Técnico a partir del censo INEGI 2020 por sección y (opcionalmente) del
histórico electoral y del índice de marginación de CONAPO:
}
\details{
\itemize{
\item \code{escolaridad_prom}: grado promedio de escolaridad (\code{graproes}).
\item \code{pct_conectividad}: proporción de viviendas con internet
(\code{vph_inter / vivpar_hab}), proxy de ingreso e infraestructura.
\item \code{tipo_seccion}: código de tipo de sección del marco (urbano/rural/
mixto según la codificación INE del año del marco).
\item \code{participacion_prom} y \code{margen_victoria_neto}: si se entrega
\code{electoral_bd} (ver \code{\link[=calcular_margen_victoria_neto]{calcular_margen_victoria_neto()}}).
\item \code{indice_marginacion}: si se entrega la tabla externa de CONAPO.
}
}
\examples{
\dontrun{
censo <- readRDS("inegi/seccion_2020.rda")
electoral <- readRDS("mex.rda")$info$bd
construir_covariables_seccion(
  censo_seccion = dplyr::filter(censo, entidad == "15"),
  electoral_bd = electoral,
  elecciones = c("pr_24", "gb_23", "pr_18")
)
}
}
