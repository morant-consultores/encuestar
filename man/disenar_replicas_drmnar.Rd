% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/drmnar_integracion.R
\name{disenar_replicas_drmnar}
\alias{disenar_replicas_drmnar}
\title{Diseño de réplicas DR-MNAR con varianza correcta (weight-swap riguroso)}
\usage{
disenar_replicas_drmnar(
  diseno,
  pregunta,
  covariables = NULL,
  instrumento = "drmnar_z",
  respuesta_ind = NULL,
  categoria = NULL,
  n_replicas = 200,
  gamma_inicial = 1.1,
  intervalo_gamma = c(-5, 8),
  semilla = 1
)
}
\arguments{
\item{diseno}{Objeto \code{survey::svydesign} del flujo de encuestar (con
pesos, UPM y estratos declarados).}

\item{pregunta}{Nombre de la variable de opinión a estimar.}

\item{covariables}{Vector de nombres de covariables (demográficas y
contextuales censales/electorales por sección). \code{NULL} usa solo la
constante.}

\item{instrumento}{Nombre de la columna con el brazo del instrumento
aleatorizado (0 = control, 1 = tratamiento).}

\item{respuesta_ind}{Nombre de la columna con el indicador de respuesta
del ítem; \code{NULL} lo deriva de los NA de la pregunta.}

\item{categoria}{Valor (o valores) de la pregunta que definen \code{y = 1},
p. ej. \code{"Sí lo conoce"}. Innecesario si la pregunta ya es 0/1.}

\item{n_replicas}{Número de réplicas de bootstrap (default 200; más
réplicas = EE más estable, más cómputo).}

\item{gamma_inicial}{Valor inicial de gamma para el solucionador.}

\item{intervalo_gamma}{Intervalo de búsqueda de gamma.}

\item{semilla}{Semilla para el remuestreo (reproducibilidad).}
}
\value{
Objeto \code{survey::svrepdesign} (tipo bootstrap) con los pesos
DR-MNAR en la réplica base y \code{n_replicas} columnas de réplica.
}
\description{
Versión de \code{\link[=ajustar_pesos_drmnar]{ajustar_pesos_drmnar()}} cuyo \code{survey::svymean}/\code{svyby}
\strong{sí} incorpora la incertidumbre de la propensión estimada \code{pi_hat}.
}
\details{
El weight-swap ingenuo de \code{\link[=ajustar_pesos_drmnar]{ajustar_pesos_drmnar()}} entrega un
\code{svydesign} cuyos pesos (diseño x \code{1/pi_hat}) \code{survey} trata como
\strong{fijos}: los errores estándar de \code{svymean} salen demasiado angostos
porque ignoran que \code{pi_hat} es estimado. Aquí, en cambio, se construye
un \code{survey::svrepdesign} con réplicas de bootstrap por conglomerado: en
cada réplica se remuestrean las UPM dentro de estrato y se \strong{re-estima
\code{pi_hat}} sobre ese remuestreo, de modo que la dispersión entre
réplicas captura toda la varianza (incluida la de la propensión).
\code{survey::svymean} sobre el resultado propaga esa varianza
automáticamente, así que morantviz reporta intervalos correctos sin
cambios.

Es la vía recomendada cuando la decisión de la pregunta es DR-MNAR y se
quieren \strong{intervalos publicables} con el flujo de morantviz. El costo
es tiempo de cómputo (una estimación de propensión por réplica).
}
\examples{
\dontrun{
rep <- disenar_replicas_drmnar(
  diseno, pregunta = "conoce_cruz", categoria = "Sí lo conoce",
  covariables = c("sexo", "rango_edad")
)
g <- morantviz::Graficar$new(diseno = rep, ...)  # EE correctos
}
}
