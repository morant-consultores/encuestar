% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registro_contactos.R
\name{pivotar_intentos}
\alias{pivotar_intentos}
\title{Pivotear los intentos de entrevista de ancho a largo}
\usage{
pivotar_intentos(
  bd,
  id = "SbjNum",
  seccion = "SECCION",
  prefijo = NULL,
  max_intentos = 15,
  codigos = NULL
)
}
\arguments{
\item{bd}{Snapshot de entrevistas (una fila por renglón levantado).}

\item{id}{Columna que identifica el renglón (default \code{"SbjNum"}).}

\item{seccion}{Columna de la sección (default \code{"SECCION"}).}

\item{prefijo}{Prefijo de las columnas de intentos. \code{NULL} (default)
autodetecta: primero \code{INT_}, luego \code{INT}.}

\item{max_intentos}{Máximo de intentos registrables (default 15, el
acuerdo con campo).}

\item{codigos}{Catálogo opcional para recodificar resultados: vector
nombrado \code{c("1" = "Efectiva", "2" = "No abrió", ...)}. Los códigos
fuera del catálogo se conservan crudos y se avisa (no se pierde
información).}
}
\value{
\code{tibble} con una fila por intento registrado: las columnas
\code{id} y \code{seccion}, \code{intento} (1..\code{max_intentos}) y \code{resultado}.
Si las columnas existen pero vienen vacías (caso Chihuahua
feb-2026), devuelve 0 filas con un aviso.
}
\description{
Convierte las columnas de intentos del snapshot (\code{INT_1..INT_15} o
\code{INT1..INT15}, una por toque de puerta hasta el máximo acordado de 15)
a formato largo: una fila por intento con su número y su resultado
(tocó y no abrió, rechazo, efectiva, ...). Es el insumo de auditoría
(verificar la metodología del encuestador contra la grabación, que
solo existe para el cuestionario) y de \code{\link[=derivar_registro_contactos]{derivar_registro_contactos()}}.
}
