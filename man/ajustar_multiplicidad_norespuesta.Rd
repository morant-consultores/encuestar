% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/norespuesta_validacion.R
\name{ajustar_multiplicidad_norespuesta}
\alias{ajustar_multiplicidad_norespuesta}
\title{Control de multiplicidad de las detecciones de no ignorabilidad}
\usage{
ajustar_multiplicidad_norespuesta(diagnostico, metodo = "BH", alfa = 0.05)
}
\arguments{
\item{diagnostico}{Tibble de \code{\link[=diagnosticar_norespuesta]{diagnosticar_norespuesta()}}.}

\item{metodo}{Método de \code{\link[stats:p.adjust]{stats::p.adjust()}} (default \code{"BH"}).}

\item{alfa}{Nivel al que se declara sobreviviente (default 0.05).}
}
\value{
El \code{diagnostico} con \code{p_valor}, \code{p_ajustado} y \code{sobrevive}, y el
atributo \code{metodo}.
}
\description{
Cada pregunta x categoría x subconjunto del diagnóstico es una prueba de
hipótesis, así que con decenas de pruebas a \code{alfa = 0.05} se esperan varias
señales por puro azar. Esta función deriva el p-valor de dos colas del
\code{z_stat} del diagnóstico y lo ajusta por multiplicidad.
}
\details{
El método se expone en vez de fijarse porque controlan cosas distintas:
\code{"BH"} controla la tasa de falso descubrimiento (FDR) y \code{"holm"} la tasa de
error por familia (FWER), que es más estricta. La diferencia entre uno y otro
es información que el lector técnico necesita, no un detalle interno.
}
