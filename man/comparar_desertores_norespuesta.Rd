% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/norespuesta_validacion.R
\name{comparar_desertores_norespuesta}
\alias{comparar_desertores_norespuesta}
\title{Contraste con los desertores del filtro temático (continuum of resistance)}
\usage{
comparar_desertores_norespuesta(bd, preguntas)
}
\arguments{
\item{bd}{\code{data.frame} del snapshot con \code{drmnar_z}, \code{drmnar_r} y las
preguntas.}

\item{preguntas}{Lista nombrada \code{list(pregunta = categoria)}.}
}
\value{
Tibble con \code{pregunta}, \code{categoria}, \code{n_voluntario}, \code{n_desertor},
\code{p_voluntario}, \code{p_desertor}, \code{diferencia} y \code{p_valor}. Cero filas si no
hay desertores con respuesta.
}
\description{
Dentro del brazo de tratamiento, compara la proporción de cada categoría
entre quienes eligieron el módulo político (\code{drmnar_r == 1}) y quienes
desertaron al menú temático (\code{drmnar_r == 0}) pero aun así traen respuesta.
}
\details{
Es una validación externa del modelo MNAR: si el signo de
\code{p_voluntario - p_desertor} coincide con el de \code{gamma_y}, dos rutas
independientes —el modelo de propensión y una diferencia de proporciones sin
modelo— llegan a la misma conclusión.
}
