% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/drmnar_preparar.R
\name{generar_diseno_drmnar}
\alias{generar_diseno_drmnar}
\title{Segundo tipo de diseño: bundle DR-MNAR para el flujo de morantviz}
\usage{
generar_diseno_drmnar(
  diseno,
  preguntas,
  covariables = NULL,
  instrumento = "drmnar_z",
  subconjuntos = NULL,
  override = NULL,
  ...
)
}
\arguments{
\item{diseno}{Objeto \code{survey::svydesign} del flujo de encuestar cuyas
variables ya incluyen el instrumento (\code{drmnar_z}) y las preguntas
(ver \code{\link[=preparar_variables_drmnar]{preparar_variables_drmnar()}}). Debe contener también a los
desertores del filtro (no respondientes del módulo político).}

\item{preguntas}{Lista nombrada \code{list(pregunta = categoria(s))}.}

\item{covariables}{Covariables individuales y/o seccionales presentes
en las variables del diseño.}

\item{instrumento}{Columna del brazo aleatorizado.}

\item{subconjuntos}{Lista nombrada de vectores lógicos para diagnóstico
por subgrupo.}

\item{override}{Tibble opcional \code{pregunta}, \code{decision} (y opcionalmente
\code{motivo}) que fuerza la decisión de esas preguntas; el diagnóstico
original se conserva para auditoría.}

\item{...}{Argumentos adicionales para \code{\link[=diagnosticar_norespuesta]{diagnosticar_norespuesta()}}.}
}
\value{
Objeto de clase \code{diseno_drmnar}: lista con \code{diseno},
\code{diagnostico}, \code{decision} (con override aplicado), \code{covariables},
\code{instrumento}, \code{override} y \code{fecha}.
}
\description{
A partir del diseño muestral ponderado del flujo normal de encuestar
(raking incluido), corre el diagnóstico de no respuesta no ignorable
por pregunta/subconjunto y arma el bundle \code{diseno_drmnar} que acompaña
al diseño en el resto del pipeline (morantviz / morantviz_factory).
}
\details{
La regla de decisión es automática (Sección 5 del Anexo Técnico:
IC 95\% de gamma excluye 0 => DR-MNAR; en caso contrario => Raking) y
admite un \code{override} manual por pregunta del analista. Raking y DR-MNAR
son mutuamente excluyentes por estimando: las preguntas con decisión
"Raking" se estiman con el diseño tal cual (svymean), y las "DR-MNAR"
con \code{\link[=estimar_drmnar]{estimar_drmnar()}} o con el diseño repesado de
\code{\link[=ajustar_pesos_drmnar]{ajustar_pesos_drmnar()}}.
}
